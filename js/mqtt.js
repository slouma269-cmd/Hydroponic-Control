const MQTT={client:null,connected:false,last:0,onData:null,onStatus:null,connect(){const c=Store.data.mqtt;if(!c.host||!window.mqtt){this.status(false);return}try{let u=c.host.trim();if(!/^wss?:\/\//.test(u))u='wss://'+u;this.client=mqtt.connect(u,{username:c.user,password:c.pass,clientId:'hydro-'+Math.random().toString(16).slice(2),reconnectPeriod:4000});this.client.on('connect',()=>{this.status(true);this.client.subscribe('hydroponic/#')});this.client.on('offline',()=>this.status(false));this.client.on('reconnect',()=>this.status(false));this.client.on('error',()=>this.status(false));this.client.on('message',(t,p)=>{this.last=Date.now();let m;try{m=JSON.parse(p.toString())}catch{m={}}if(this.onData)this.onData(t,m)})}catch{this.status(false)}},status(v){this.connected=v;if(this.onStatus)this.onStatus(v)},publish(t,p){if(this.connected)this.client.publish(t,typeof p==='string'?p:JSON.stringify(p))}};
