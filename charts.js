const Charts={metric:'temperature',range:86400000,draw(){const c=document.getElementById('chart'),x=c.getContext('2d'),a=App.history.filter(v=>Date.now()-v.ts<=this.range),vals=a.map(v=>+v[this.metric]).filter(Number.isFinite);x.clearRect(0,0,c.width,c.height);if(!vals.length)return;const w=c.clientWidth,h=230,d=devicePixelRatio||1;c.width=w*d;c.height=h*d;x.scale(d,d);const mn=Math.min(...vals),mx=Math.max(...vals);x.strokeStyle='#0b9b68';x.lineWidth=3;x.beginPath();a.forEach((v,i)=>{let n=+v[this.metric];if(!Number.isFinite(n))return;let px=20+i/Math.max(1,a.length-1)*(w-30),py=h-25-(n-mn)/Math.max(.001,mx-mn)*(h-50);i?x.lineTo(px,py):x.moveTo(px,py)});x.stroke()}};
